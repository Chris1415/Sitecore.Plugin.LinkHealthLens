# Product Requirements Document

---
document_type: prd
prd_id: PRD-000
artifact_name: prd-000.md
generated_at: 2026-08-31T15:04:00Z
run_manifest: project-planning/workflow/run-20260831T143222Z.json
source_inputs:
  - project-planning/plans/interrogation-discovery-20260831T143222Z.md
consumed_by:
  - Product Manager Agent (critical mode — /challenge-prd)
  - Software Architect Agent (ADRs only — track is `minimal`, no architecture blueprint)
next_input:
  - project-planning/specs/prd-000.md
---

<!-- PRD numbering: PRD-000 = holistic view of the whole app (greenfield starting point). Subsequent PRDs are PRD-001, PRD-002, ... ascending. -->

## 1. Executive Summary

**Link Health Lens** is a custom Sitecore Marketplace app that lives in the SitecoreAI Page Builder
**context panel** and gives an editor a pre-publish glance at the **internal link integrity** of the
page they are editing. It answers one question before the publish button gets pressed: *do the links
on this page point at content that actually exists and is actually live?*

It is **not a broken-link checker and must never present itself as one.** The app runs under the
Marketplace **Mode A** auth model — plain client-side, inside a sandboxed iframe where every call is
brokered by the portal — so it structurally cannot issue a request to an arbitrary origin. External
reachability (4xx/5xx, redirect chains, DNS failure) is therefore **permanently out of scope**, not
deferred (ledger Q5). What it ships instead is five checks it can perform honestly and offline:
internal link existence in the CM, links aimed at staged-but-unpublished pages, `http://` scheme
detection, malformed or empty `href`, and in-page anchors whose target is missing from the page's own
HTML.

Scope is the **rendered** page including nav and footer chrome, with best-effort attribution back to
an owning field; links that cannot be attributed are labelled `site chrome — not editable from this
page` so the editor stops looking rather than chasing a dead affordance.

## 1.5 Developer slice (extracted to prd-minimal-NNN.md)

<!-- PRD_MINIMAL_BEGIN -->

> The Product Manager authors this section as the authoritative Developer-08 brief.
> /create-prd extracts everything between PRD_MINIMAL_BEGIN/END verbatim into
> `prd-minimal-NNN.md` at the close of the command. Manual edits to
> `prd-minimal-NNN.md` are overwritten on the next extraction — edit here only.

### Problem (one short paragraph)

An editor working in SitecoreAI Page Builder has no way to see, before publishing, whether the links
on the page point at content that exists in the CM and is actually published. Bad internal links are
discovered after go-live, by visitors. The signal exists in the tenant — the editor just has no
surface that shows it next to the page they are editing.

### Goal (one short paragraph)

Ship a read-only Marketplace context-panel app that, on every page selection in Page Builder, lists
every link in the rendered page and classifies each one against five internal-integrity checks, with
a jump-to-field action where the link can be attributed to an editable field and an explicit
`site chrome — not editable from this page` label where it cannot.

### Non-negotiables (bullets)

- **Mode A (plain client-side) only.** No backing route, no server-side surface, no OAuth proxy — not
  now, not later. Scaffold via the pack-resolved command
  `npx shadcn@latest add https://blok.sitecore.com/r/marketplace/next/quickstart-with-client-side-xmc.json`
  (cited from the pack brief; do not substitute a Mode B quickstart).
- **No outbound network request to any link target, ever.** Every check is CM-data or static-string
  based. A `fetch`/`HEAD` against a link href is an automatic Critical at code review.
- **The UI must never call a link "broken", "dead", "404" or "unreachable".** External links are
  listed and labelled as *not checked*. This is a product-integrity constraint, not copy preference.
- **Read-only.** The app performs no write, mutation or publish against the tenant.
- **No persistence.** Findings are computed per page-selection event and held in memory only; no
  storage, no cache that survives a reload, no telemetry.
- Exact label string for unattributable links: `site chrome — not editable from this page`.
- SDK request/response shapes are **derived from `node_modules` `.d.ts`** (rule `40`) and cited inline.
  Nothing is scaffolded yet, so every shape below is **unverified** and gets captured in tranche 1.

### In scope / out of scope (very short)

- **In scope:** the rendered page in the Page Builder canvas, including nav/footer chrome. Five
  checks — (1) internal link target exists in CM, (2) target exists but is unpublished, (3) `http://`
  scheme, (4) malformed/empty `href`, (5) in-page anchor with no matching target in the page's own
  HTML. Internal-vs-external classification from the tenant's own host list. Best-effort field
  attribution + jump action. Two divergent panel designs at `/design`, one POC winner.
- **Out of scope:** every outbound network check (4xx/5xx, redirect chains, DNS, mixed-content-by-
  response) — permanent, not deferred. Full attribution for every chrome link. Any HEAD-proxy
  concurrency or rate-limit policy (there is no proxy). Site-wide or multi-page scanning. Any write
  to the tenant. Public Marketplace listing / submission.

### Success criteria (3–7 bullets)

- Selecting a page in Page Builder renders a complete finding list in ≤ 3s (p50) / ≤ 6s (p95) for a
  page with ≤ 150 anchors.
- Every anchor present in the page HTML appears in the panel exactly once, matched by
  `href` + document ordinal — **a matching total count is not a reconciliation**; match pairwise.
- ≥ 50% of non-chrome links carry a working jump-to-field action (Assumption A3's threshold).
- The tranche 3 real-tenant smoke runs against **both** a known-bad page and a **known-good control
  page**, and reports zero findings on the control. A uniform-failure sweep proves nothing.
- Zero occurrences of "broken" / "dead" / "404" / "unreachable" applied to external links in shipped
  UI copy (grep-able at `/code-review`).

### Key constraints & assumptions

- **Mode A is architectural and load-bearing** — it is what removes external checking. ADR candidate
  (see § 9); do not re-open it in implementation.
- **A1 (assumption):** the page-HTML endpoint returns *rendered HTML*, not layout JSON. If it returns
  layout, link extraction is a different piece of work and tranche 2 is reshaped. Probe first.
- **A2 (assumption):** `path-by-url` can distinguish *page missing* from *page exists but
  unpublished*. This is a headline feature and nothing confirms the endpoint separates them. Probe
  first; do not build the unpublished check on top of an unconfirmed distinction.
- **A3 (assumption):** chrome links attribute to an owning datasource often enough that "best-effort"
  reads as partial rather than blank. Below ~50% the feature reads as broken.
- **Five SDK surfaces, all unverified:** `pages.context` (subscribe) · the page-HTML endpoint
  (`xmc.pages.*` **or** `xmc.agent.*` — which one is itself unresolved) · `path-by-url` ·
  `xmc.sites.*` (hosts) · `application.context`. Capture real shapes + fixtures in tranche 1.
- **The tenant-facing `app_name` must be settled before `/implement`** — Cloud Portal App Studio
  registration precedes the first `ClientSDK.init()` handshake. Registration is **operator-guided and
  machine-verified; there is no registration API** (probe 0a, 2026-08-15).
- **Sibling reference is `cite-required`:** `quickcopy`, `component-usage-atlas` and `pageshot` each
  solved part of the context-panel problem. Borrowing is allowed; every borrowed pattern is logged in
  `docs/build-decisions.md` with the sibling and file it came from.

### Handoff

- **Full PRD:** `project-planning/specs/prd-000.md` (for humans and upstream agents only — not loaded
  by agent 08 in normal flow.)
- **Executable contract:** `project-planning/plans/task-breakdown-<timestamp>.md` after QA (07)
  enrichment.

<!-- PRD_MINIMAL_END -->

## 2. Problem Statement

Editors publish pages with internal links that point at nothing. The target item was renamed, moved,
never created, or — most commonly and most invisibly — **created but never published**, so the link
works perfectly for the editor previewing it and 404s for the visitor. The information needed to
catch all of this already lives in the tenant, but the editor has no surface that shows it beside the
page they are editing. The check happens after go-live, by a visitor, or not at all.

**What this app deliberately does not solve.** The original framing of this idea was external link
rot — "angry visitors hitting dead outbound links". The auth decision in ledger Q4 (Mode A, plain
client-side) makes that structurally impossible: inside the sandboxed iframe every call is
portal-brokered, so the app cannot issue a request to an arbitrary origin. Rather than carry a
promise the architecture cannot keep, the problem was **rewritten** in ledger Q5 to the half the app
can actually deliver: internal link integrity, checked against the CM, with no network request to any
link target.

This constraint is stated up front and repeated in the UI because the failure mode is not a missing
feature — it is a **false sense of coverage**. An editor who believes the panel checked their
outbound links, and it did not, is worse off than an editor with no panel at all.

## 3. Goals and Success Metrics

**Goals**

- **G1** — Let an editor see, without leaving Page Builder, whether the internal links on the current
  page resolve to real, published content.
- **G2** — Make the boundary of the check obvious, so nobody infers external coverage that does not
  exist.
- **G3** — Where a finding is fixable from this page, take the editor to the field; where it is not,
  say so plainly instead of offering a dead affordance.

**Success metrics**

| # | Metric | Target | Measured at |
|---|--------|--------|-------------|
| M1 | Time from page-selection event to complete finding list | ≤ 3s p50 / ≤ 6s p95, page with ≤ 150 anchors | tranche 3 exit |
| M2 | Anchor coverage — every anchor in the page HTML appears once in the panel, matched by `href` + document ordinal (pairwise, not by total count) | 100% | tranche 2 exit |
| M3 | Attribution rate — non-chrome links carrying a working jump-to-field action | ≥ 50% | tranche 4 exit |
| M4 | False positives on a **known-good control page** in the real-tenant smoke | 0 | tranche 3 exit |
| M5 | UI copy applying "broken" / "dead" / "404" / "unreachable" to an external link | 0 occurrences | every `/code-review` |

M4 is deliberate: a sweep that reports failures everywhere is a claim about the checker, not about
the content. The smoke must include a page whose links are known to be sound and report nothing on it.

## 4. Target Users

**Primary — the content editor in Page Builder.** Works inside the canvas, is about to publish, is
not a developer, and has no access to the content tree in the mental model they are using. They need
a verdict and a route to the fix, phrased in their vocabulary ("this link points at a page that isn't
published yet"), not in template/item terms.

**Secondary — the site owner / lead editor.** Cares about the pattern rather than the page: which
chrome links are wrong everywhere because they are wrong once. The rendered-page scope (ledger Q2)
means they will see the same chrome finding on every page — accepted cost, and the reason the
`site chrome` label exists.

**Not a user:** the developer debugging the head app, and the SEO analyst auditing outbound links.
Neither is served by this app and neither should be told otherwise.

## 5. Product Scope

### In Scope

- **Surface:** SitecoreAI Page Builder **context panel**, custom app, our org only — no public
  Marketplace listing, no Sitecore review gate, no submission step (ledger Q1).
- **Page scope:** the **rendered** page as the editor sees it, **including nav and footer chrome**
  (ledger Q2). Accepted cost: some findings are not fixable from the page being viewed.
- **Five checks:**
  1. **Internal target exists** — the link resolves to an item in the CM.
  2. **Unpublished target** — the item exists but is not published to the delivery surface.
  3. **`http://` scheme** — static string inspection, no request issued.
  4. **Malformed or empty `href`** — empty, whitespace-only, unparseable, or a literal placeholder.
  5. **Missing in-page anchor** — `href="#x"` with no `id`/`name` `x` in the page's own HTML.
- **Internal vs external classification** from the tenant's own host list, so an external link is
  identified as external and then **listed but not checked**, with a visible not-checked label.
- **Best-effort field attribution** — links that map to an owning field get a jump action; the rest
  render as `site chrome — not editable from this page` (ledger Q3).
- **Two divergent panel designs** at `/design`, one POC winner (`ui_variants: 2`).

### Out of Scope

**Permanently out of scope — architectural, not a backlog item:**

- Every outbound network check: HTTP status (4xx/5xx), redirect chains, DNS failure, TLS validity,
  mixed-content-by-response. Mode A cannot issue the request (ledger Q5).
- Any HEAD proxy, and therefore any concurrency or rate-limit policy for one — moot, there is no proxy.
- Any backing route, server-side surface or OAuth proxy.

**Out of scope for this PRD:**

- Full source attribution for every chrome link — the "keep the promise" option was offered in
  discovery and explicitly not taken.
- Site-wide or multi-page scanning; scheduled or background runs.
- Any write, mutation, publish or redirect-creation action against the tenant.
- Persisted history, trend reporting, export, or telemetry.
- Public Marketplace listing and its interface-guidelines bar (a preference here, not a requirement).

## 6. User Stories

**US-1 — See every link on the page I'm editing.**
*As an editor, I want the panel to list every link on the page I have selected, so that I have one
place to look before publishing.*

- **AC-1.1** Selecting a page in Page Builder populates the panel without a manual refresh.
- **AC-1.2** Every anchor in the rendered page HTML — body, nav and footer — appears exactly once,
  matched by `href` + document ordinal.
- **AC-1.3** Switching to a different page replaces the list; no findings from the previous page survive.
- **AC-1.4** A page with zero links renders an explicit empty state, not a blank panel.

**US-2 — Know which internal links point at nothing.**
*As an editor, I want internal links whose target does not exist in the CM flagged, so that I don't
publish a link to a page that was never created or has been moved.*

- **AC-2.1** A link resolving to no CM item is flagged as *target not found* with its href shown.
- **AC-2.2** A link resolving to an existing item is not flagged by this check.
- **AC-2.3** When the target cannot be resolved either way (API error, timeout), the link is shown as
  *could not check* — never silently as "fine" and never as "not found".

**US-3 — Know which links point at a page that isn't published yet.**
*As an editor, I want links to staged-but-unpublished pages flagged distinctly, so that I don't ship
a link that works for me in preview and 404s for a visitor.*

- **AC-3.1** *Unpublished target* is a **distinct** finding class from *target not found*, with its
  own label and its own copy.
- **AC-3.2** The finding names the target page, so the editor can go publish it.
- **AC-3.3** If tranche 1 proves the endpoint cannot separate the two states (Assumption A2), the
  merged state is labelled honestly as *not found or not published* — it is never presented as one of
  the two when it is actually both.

**US-4 — Be warned about insecure-scheme links.**
*As an editor, I want `http://` links flagged, so that I don't ship a link that will be blocked or
downgraded on an https site.*

- **AC-4.1** Any href whose scheme is `http:` is flagged, internal or external.
- **AC-4.2** The check is performed on the string only — no request is issued and no response is
  interpreted.
- **AC-4.3** `https:`, protocol-relative, relative, `mailto:`, `tel:` and `#`-only hrefs are not
  flagged by this check.

**US-5 — Be warned about malformed or empty links.**
*As an editor, I want empty or unparseable hrefs flagged, so that I catch a link that renders as
clickable but goes nowhere.*

- **AC-5.1** Empty, whitespace-only, and unparseable hrefs are flagged as *malformed link*.
- **AC-5.2** A literal placeholder value (e.g. `#`, `undefined`, `null`) is flagged, not treated as a
  valid target.
- **AC-5.3** An anchor with no `href` attribute at all is reported, not skipped.

**US-6 — Know when an in-page anchor has no target.**
*As an editor, I want `#section` links whose target is missing from this page flagged, so that my
jump links actually jump.*

- **AC-6.1** `href="#x"` with no element carrying `id="x"` or `name="x"` in the page HTML is flagged.
- **AC-6.2** A matching target anywhere in the page — including chrome — satisfies the check.
- **AC-6.3** The check runs against the page's own HTML only; a `#`-fragment on a link to a
  *different* page is not evaluated.

**US-7 — Get to the field, or be told I can't.**
*As an editor, I want a jump action on findings I can fix here and an honest label on the ones I
can't, so that I stop looking instead of hunting for a field that isn't on this page.*

- **AC-7.1** A link attributed to an owning field renders a jump action that selects that field in
  the canvas.
- **AC-7.2** A link that cannot be attributed renders the exact string
  `site chrome — not editable from this page` and **no** jump action.
- **AC-7.3** No finding renders a jump action that does not resolve to a selectable field.
- **AC-7.4** The attributed / unattributed split is visible at a glance without opening each finding.

**US-8 — Understand what this app does not check.**
*As an editor, I want the panel to be explicit that external links are not verified, so that I don't
infer coverage the app cannot provide.*

- **AC-8.1** Every external link is listed and carries a visible *not checked* label (exact copy set
  at `/design`).
- **AC-8.2** No UI copy applies "broken", "dead", "404" or "unreachable" to an external link.
- **AC-8.3** The panel states its scope — internal link integrity — somewhere permanently visible,
  not only in a dismissible first-run message.

## 7. Functional Requirements

**Acquisition and lifecycle**

- **FR-1** Subscribe to `pages.context` and recompute the finding list on every page-selection change.
- **FR-2** Retrieve the current page's rendered HTML via the page-HTML endpoint and extract every
  anchor with its href and document ordinal. *(Shape unverified — see Assumption A1.)*
- **FR-3** Read `application.context` for app identity/host wiring at init.
- **FR-4** Discard all state on page change; hold nothing across a reload.

**Classification**

- **FR-5** Retrieve the tenant's site host list via `xmc.sites.*` and classify each link as internal
  or external against it. A link that matches no known host and is not relative is external.
- **FR-6** Resolve each internal link via `path-by-url` and classify as *exists* / *not found* /
  *exists-but-unpublished*. *(The third depends on Assumption A2.)*
- **FR-7** Flag `http:`-scheme hrefs by string inspection.
- **FR-8** Flag empty, whitespace-only, unparseable and placeholder hrefs.
- **FR-9** Flag `#`-fragment links with no matching `id`/`name` in the page HTML.
- **FR-10** Represent an unresolvable check as an explicit *could not check* state — never as a pass.
- **FR-11** List external links without checking them and label them *not checked*.

**Attribution and presentation**

- **FR-12** Best-effort map each link to an owning field; expose a jump action where the mapping
  resolves to a selectable field.
- **FR-13** Render `site chrome — not editable from this page` for unattributable links, with no
  jump action.
- **FR-14** Group and order findings so the fixable-here set is reachable without scrolling past the
  chrome set (exact grouping decided at `/design`).
- **FR-15** Render distinct empty, loading and error states.

**Prohibitions (testable)**

- **FR-16** The app issues **no** network request to any link target. Any `fetch`/`XHR`/`HEAD` against
  an extracted href is a defect.
- **FR-17** The app performs **no** write, mutation or publish against the tenant.

## 8. Non-Functional Requirements

- **NFR-1 Performance** — M1's budget (≤ 3s p50 / ≤ 6s p95 at ≤ 150 anchors). Resolution calls are
  de-duplicated by target path; the same href appearing 20 times costs one lookup.
- **NFR-2 Partial results over blank** — if one classification source fails, findings from the others
  still render, with the failed class marked *could not check*. The panel never blanks because one
  call failed.
- **NFR-3 Read-only and side-effect-free** — no writes, no persistence, no telemetry, no cookies.
- **NFR-4 Panel fit** — usable at the context panel's actual width without horizontal scrolling;
  long hrefs truncate with the distinguishing end visible.
- **NFR-5 Accessibility** — keyboard-reachable findings and jump actions; findings are not conveyed
  by colour alone. Blok interface guidelines are a **preference** here, not a gate (ledger Q1).
- **NFR-6 Honesty of copy** — enforced as M5, checked at every `/code-review`.

## 9. Technical Considerations

`track: minimal` — there is **no architecture blueprint**. Architectural commitments are recorded as
ADRs by the Architect; this section carries the weight that a blueprint otherwise would.

**Auth model and scaffold.** `variables.mode = client-side` (resolved by ledger Q4). The pack brief
resolves the scaffold command to
`npx shadcn@latest add https://blok.sitecore.com/r/marketplace/next/quickstart-with-client-side-xmc.json`
— cited from the pack, run literally per rule `50`, not reconstructed. Mode A means portal-brokered
calls only: no backing route, no server-side surface, no OAuth proxy. This single decision is what
removes external checking, so it is the app's most load-bearing constraint.

**SDK surfaces — five, all UNVERIFIED.** Rule `40` requires request/response shapes to be derived
from `node_modules` `.d.ts` and cited inline. **Nothing is scaffolded yet**, so no shape in this
document is verified and none may be treated as known. All five are captured in tranche 1:

| Surface | Used for | Status |
|---------|----------|--------|
| `pages.context` (subscribe) | current page identity + selection events | **unverified** — payload shape to be captured in tranche 1 |
| page-HTML endpoint — `xmc.pages.*` **or** `xmc.agent.*` | the rendered page markup | **unverified** — *which endpoint* is itself unresolved; see A1 |
| `path-by-url` | internal target existence, publish state | **unverified** — the missing/unpublished distinction is A2 |
| `xmc.sites.*` | tenant host list for internal/external split | **unverified** — host shape to be captured |
| `application.context` | app identity at init | **unverified** |

No fixture, type or call signature in the task breakdown may be written from training data or from a
skill catalogue. Tranche 1 produces the fixtures; everything downstream cites them.

**Registration and naming.** There is **no registration API** — probe 0a (2026-08-15) refuted it: the
Marketplace SDK CLI scaffolds code only, and the capability is gated on an Org Admin/Owner *user*
role. Registration is **operator-guided and machine-verified**, and there is no
`/provision-marketplace` stage. `workspace.naming.required_at = before_scaffold`: the tenant-facing
`app_name` is **not required yet**, but must be settled before `/implement`, because App Studio
registration precedes the first `ClientSDK.init()` handshake. Tracked as a release-plan step (§ 12),
**not** an open blocker.

**Repo and workspace.** `workspace.container = site/`, laid out at
`products/marketplace/link-health-lens/site`. `repo_setup.deployable_baseline = null` — nothing builds
from `main` on this platform, so there is no baseline scaffold at intake and `/ship` does **not**
split the PR.

**Sibling reference — `cite-required`.** `quickcopy`, `component-usage-atlas` and `pageshot` each
solved part of the context-panel problem (panel shell, page-context subscription, canvas
interaction). Referencing them is allowed and encouraged; **every borrowed pattern is logged** in
`docs/build-decisions.md` naming the sibling product and the file it came from. An unlogged borrowed
pattern is a code-review finding.

**ADR candidates** (flagged here; the Architect writes them):

- **ADR-a** — Mode A client-side only, permanently no backing route. Records the consequence: external
  reachability is unreachable by construction.
- **ADR-b** — Link source of truth: rendered HTML vs layout JSON (decided by the tranche 1 probe).
- **ADR-c** — Internal/external classification strategy: tenant host list vs path heuristic.
- **ADR-d** — Attribution strategy and its fallback label, including the below-50% failure posture.
- **ADR-e** — No persistence: recompute per page-selection event, hold nothing.

## 10. Data Model Overview

The app **creates no Sitecore templates, items or fields** and persists nothing. The only model is an
in-memory, per-page-selection structure:

- **`LinkFinding`** — `href` (raw, as authored) · `ordinal` (document position, part of the identity
  with `href`) · `scope` (`internal` | `external`) · `origin` (`content` | `chrome`) ·
  `status` (`ok` | `not-found` | `unpublished` | `not-found-or-unpublished` | `malformed` |
  `insecure-scheme` | `missing-anchor` | `not-checked` | `could-not-check`) · `severity` ·
  `attribution` (owning field reference, or null) · `targetLabel` (resolved page name, when known).
- **`PageScan`** — the page identity from `pages.context`, the ordered `LinkFinding[]`, per-source
  health flags backing NFR-2, and a completion timestamp.

`not-found-or-unpublished` exists only as the honest merged state if Assumption A2 is refuted; if the
probe confirms the distinction, it is never emitted. Field names above are indicative — the real
shapes are constrained by the tranche 1 fixtures.

## 11. UX Considerations

- **Surface constraints.** The context panel is narrow and vertical, inside the Page Builder canvas.
  Findings must be scannable at a glance; the panel competes with the page for the editor's attention
  and loses if it needs study.
- **The honest-boundary problem is a UX problem.** The scope statement (AC-8.3) has to be permanently
  visible without dominating the panel — this is one of the things the two `/design` variants should
  diverge on.
- **Two divergent proposals** (`ui_variants: 2`), one POC winner, and the `design` gate is the single
  operator stop in this run. Suggested axis of divergence: *grouped by severity* (worst first,
  chrome folded away) vs *grouped by fixability* (fixable-here first, chrome as a labelled tail).
- **The `site chrome — not editable from this page` label is load-bearing** and its exact string is
  fixed (ledger Q3). It must read as an answer, not as a disabled button.
- **States:** empty (no links), loading (page changed, results pending), partial (NFR-2), error.
  Loading must not look like "clean".
- Phase 4 closes with a **pixel compare against the POC winner** — the POC is the implementation
  contract for the visual layer, not a mood board.

## 12. Release Plan

Single release, four sequential phases (§ 12a). `delivery_loop: per_component`,
`delivery_parallelism: serial` — one tranche fully delivered, reviewed and tested before the next
opens.

**Scope dial in force**

| Dial | Value |
|------|-------|
| `rigor` | `light` |
| `track` | `minimal` (no architecture blueprint; ADRs only) |
| `ui_variants` | `2` |
| `docs_at_ship` | `auto-lite` |
| `delivery_loop` | `per_component` |
| `delivery_parallelism` | `serial` |
| `external_reference` | `cite-required` |
| `gates` | **`design` only** — `spec`, `build`, `final`, `go_live` are off |
| `content_authoring` | `null` — filtered out by pack; no active pack owns `/author-content` |
| `imagery` | `null` — filtered out by pack |

`content_authoring` and `imagery` are `null` **by pack filter, not by default**; no value is assumed
for them and no content phase runs.

**Release steps**

1. `/challenge-prd` (chained) → `/architect` (ADRs only) → `/design` (**the one operator gate**) →
   `/task-breakdown` → phases 1–4 → `/ship`.
2. **Before `/implement`:** settle the tenant-facing `app_name` and complete **operator-guided**
   App Studio registration in Cloud Portal, then **machine-verify** it. Registration precedes the
   first `ClientSDK.init()` handshake. This is a scheduled step, not an open blocker.
3. `/ship` writes README + CHANGELOG on the `auto-lite` path and does **not** split the PR
   (`deployable_baseline = null`).
4. There is **no** `/publish` stage and no content phase for this platform.

## 12a. Execution tranches (probe-first)

The SDK-surface heuristic fired — **5 detected surfaces against a threshold of 3** — and resolved to
**phased inside this one PRD**. This is not a split: there is no program block, no member count, and
no follow-up PRD. Each phase closes with a probe verdict or a real-tenant smoke.

| Phase | Scope (SDK surface / concern cluster) | Tranches | Exit verification | Unblocks |
|-------|--------------------------------------|----------|-------------------|----------|
| 1 | 🔍 **Probe — blocking, runs first and alone.** All five surfaces against a real tenant | 🔍 T0 | Captured fixtures for every surface, **or** a refutation that reshapes phase 2 | phase 2 |
| 2 | `pages.context` + page-HTML endpoint → extraction | T1 | Real-tenant render: the raw anchor list rendering inside the canvas; M2 anchor coverage holds | phase 3 |
| 3 | `path-by-url` + `xmc.sites.*` → classification | T2 | Real-tenant smoke against a page with **known-bad** links **and** a **known-good control** (M4 = 0) | phase 4 |
| 4 | Attribution + UI | T3 | Pixel compare against the POC winner; M3 attribution rate ≥ 50% | ship |

**This PRD is complete when all four phases are delivered** — a shipped phase is progress, not the
deliverable.

**Phase 1 probe — the four questions it must answer** (rule `70`; a refutation is a valid, even
preferred, outcome):

- **(a)** Does the page-HTML endpoint (`xmc.pages.*` or `xmc.agent.*`) return **rendered HTML** or
  **layout JSON**? → resolves **A1**. Layout JSON reshapes phase 2 entirely.
- **(b)** Can `path-by-url` separate *page missing* from *page exists but unpublished*? → resolves
  **A2**. A refutation collapses US-3 to the honest merged state (AC-3.3), it does not drop the feature.
- **(c)** What is the `xmc.sites.*` host shape, and is it sufficient for internal-vs-external
  classification? → feeds **ADR-c**.
- **(d)** What is the `pages.context` subscription payload, and does it fire on every selection change?

Phase 1 is **blocking and runs alone**. No phase-2 work commits against an unconfirmed shape.

## 13. Risks and Mitigations

| # | Risk | Impact | Mitigation |
|---|------|--------|------------|
| R1 | **A1 refuted** — the endpoint returns layout JSON, not HTML | Phase 2 is a different piece of work; anchor extraction from layout may not see chrome at all, which would contradict the rendered-page scope (ledger Q2) | Phase 1 probes it first and alone; a refutation triggers a scope re-decision with the operator before phase 2 opens |
| R2 | **A2 refuted** — *missing* and *unpublished* are indistinguishable | The headline feature (US-3) degrades to a merged state | AC-3.3 pre-commits the honest fallback label; the feature is not silently dropped and not silently mislabelled |
| R3 | **A3 misses** — attribution lands below ~50% | The panel reads as broken rather than partial | Measured as M3 at phase 4 exit; below threshold, escalate to the operator as a UX re-decision (fold chrome away by default, or re-open full attribution) |
| R4 | **Name/scope tension** — "Link Health Lens" implies more than internal integrity | Users infer external coverage that does not exist | Raised explicitly in discovery and the name was **kept**; mitigated in-product by AC-8.1–8.3 and M5, not by renaming |
| R5 | Performance on link-dense pages (large nav/footer) | Panel feels slow, editors stop using it | NFR-1 de-duplication by target path; M1 measured at phase 3 exit |
| R6 | Registration blocks `/implement` — no API, human role gate | Build stalls at the container boundary | Scheduled explicitly as release step 2, ahead of `/implement`; operator-guided, machine-verified |
| R7 | Chrome findings repeat on every page (accepted cost of ledger Q2) | Perceived noise | The `site chrome` label plus phase-4 grouping; if noise dominates, grouping — not scope — changes |

## 14. Open Questions

- **OQ-1** Which endpoint family serves page HTML — `xmc.pages.*` or `xmc.agent.*`? *(Phase 1 probe (a).)*
- **OQ-2** Exact copy for the external *not checked* label and the permanent scope statement.
  *(Decided at `/design`.)*
- **OQ-3** Is a link inside a rendered component with a datasource attributable to that datasource's
  field, or only to the component? *(Bounds M3; phase 4.)*
- **OQ-4** Does `pages.context` fire on in-canvas edits, or only on page selection — i.e. does the
  panel go stale while the editor types? *(Phase 1 probe (d).)*
- **OQ-5** The tenant-facing `app_name`. *(Required before `/implement`, release step 2.)*

## 15. Future Opportunities

- Full source attribution for every chrome link — the "keep the promise" option offered in discovery
  and not taken (ledger Q3).
- Site-wide scan across all pages of a site, so a chrome finding is reported once rather than per page.
- Awareness of the redirect map: a link to a moved item that is covered by a redirect is a different
  (and lesser) finding than one that is not.
- Export or share of a page's findings for hand-off to whoever owns the chrome.
- Editor-configurable severity — treat `http://` as informational on sites that still serve it.

**Deliberately absent.** External reachability checking (4xx/5xx, redirect chains, DNS) is **not** a
future opportunity and is not listed above. It is permanently out of scope under Mode A (§ 5, ADR-a);
adding it would require reversing the auth-model decision, which is a new product conversation, not a
backlog item.

## Handoff Metadata
- Canonical run manifest: `project-planning/workflow/run-20260831T143222Z.json`
- Source concept brief: none — discovery ran inline at `/create-prd`; ledger at
  `project-planning/plans/interrogation-discovery-20260831T143222Z.md`
- Recommended next command: `/challenge-prd` (auto-chained)
- Recommended next input file: `project-planning/specs/prd-000.md`
