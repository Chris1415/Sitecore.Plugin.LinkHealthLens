# Project Status — link-health-lens

_Auto-generated by `/create-prd` kickoff and `/ship` close. Do not hand-edit — overwritten on next regeneration. To force a refresh, delete this file; the next `/create-prd` or `/ship` regenerates it. To remove permanently, run `/cleanup-project-planning`._

**Generated:** `2026-08-31T14:45:00Z`
**Repo:** `https://github.com/Chris1415/Sitecore.Plugin.LinkHealthLens.git`
**Composition:** `marketplace` × `greenfield` × `developer` (from the manifest's `packs` block)
**Latest manifest:** `project-planning/workflow/current-run.json`

## Work item roll-up

| Item | Type | Title | Status | Stage | Smoke | PR(s) | Manifest |
|------|------|-------|--------|-------|-------|-------|----------|
| PRD-000 | prd | Internal-link integrity panel for the SitecoreAI Page Builder | in_progress | `spec_ready` — PRD + prd-minimal + 3 ADRs written; `/challenge-prd` next | n/a | — | `run-20260831T143222Z.json` |

## Open items

- `[warn]` create-prd › decision › The tenant-facing `app_name` is unresolved and Cloud Portal App Studio registration must happen **before `/implement`** — there is no registration API (probe 0a, refuted 2026-08-15), so this is operator-guided by construction and stops the run regardless of `gates.go_live` being off (raised today, PRD-000)
- `[warn]` create-prd › assumption › **A2** — `path-by-url` can distinguish *page missing* from *page exists but unpublished*. Unconfirmed, and US-3 is a headline feature resting on it; AC-3.3 pre-commits a merged label so a refutation degrades honestly (raised today, PRD-000)
- `[warn]` create-prd › assumption › **A1** — the page-HTML endpoint returns rendered HTML, not layout JSON. If it returns layout, tranche 2 is reshaped (raised today, PRD-000)
- `[warn]` create-prd › assumption › **A3** — chrome-link attribution reaches ~50%, the line between "partial" and "broken" (raised today, PRD-000)
- `[warn]` create-prd › concern › Name/scope tension kept deliberately: the app is called *Link Health Lens* and never checks an external link's health. Mitigated in-product by metric M5, not by renaming (raised today, PRD-000)
- `[warn]` create-prd › concern › PRD-000 is 560 lines against the ~400 soft cap; flagged rather than trimmed, because trimming would have cut acceptance criteria (raised today, PRD-000)
- `[warn]` create-prd › concern › `gates.spec` is off, so `/challenge-prd` interrogates and closes **soft** — the spec reaches `/architect` with no human confirmation step (raised today, PRD-000)

## Smoke status

- PRD-000 `tranche-1-probe`: pending — blocking probe of all five SDK surfaces (page-HTML shape, `path-by-url` missing-vs-unpublished, `xmc.sites.*` hosts, `pages.context` payload). Runs first and alone.

## Last activity

`2026-08-31` — `/start-flow` → `/create-prd`. Composition resolved at M0 rung 4 (ask); discovery ran `light` (5 questions, 2 push-backs) and **changed the product**: Mode A removes every outbound network check, so this is internal-link integrity, not broken-link detection (ADR-0002). SDK-surface heuristic fired at 5 surfaces ≥ threshold 3 → **phased inside one PRD**, 4 tranches, probe-first.
