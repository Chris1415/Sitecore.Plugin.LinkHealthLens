{
  "schema_version": 8,
  "run_id": "20260831T143222Z",
  "created_at": "2026-08-31T14:32:22Z",
  "updated_at": "2026-08-31T17:25:00Z",
  "product_slug": "link-health-lens",
  "product_root": "products/marketplace/link-health-lens",
  "products_root_abs": null,
  "product_root_abs": null,
  "// products root is a MOUNT POINT (product-workspace.md § 1a)": [
    "product_root and every canonical_artifacts path stay LOGICAL — they always start with products/, whatever disk the trees are actually on. Resolve with .agent/scripts/resolve-products-root.mjs; never store an absolute path in a logical field, or this manifest stops being readable on another machine.",
    "products_root_abs / product_root_abs are DERIVED, per-machine mirrors, refreshed by whichever command touches the manifest. They exist so slim-context agents (Developer 08, which never reads product-workspace.md) get absolute paths and resolve nothing. Never trust a stale value carried over from another machine — rewrite it and continue."
  ],
  "deploy_project_name": null,
  "project_slug": null,
  "project_root": null,
  "cm_code_base": null,
  "// two-level identity (schema 8+, when the platform pack declares the two-level repo_setup.topology)": [
    "For content-sdk head-app runs (product-workspace.md § 1b-i / § 3): product_slug + product_root point at the HEAD APP — the pipeline-run workspace where project-planning/ lives, e.g. products/head-application/<project-slug>/<head-app-slug>.",
    "The four fields above name the Deploy PROJECT (= one CM server) the head app belongs to: deploy_project_name = the verbatim Deploy project name (spaces/caps allowed, e.g. 'CHAH DevEx Journey'); project_slug = the space-free project folder; project_root = products/head-application/<project-slug>; cm_code_base = <project_root>/cm (NEW CM) or the operator's existing CM path (EXISTING CM, no cm/ created).",
    "All head apps under one project share ONE cm_code_base + ONE Deploy project. Leave these null for marketplace / personal / legacy-flat products."
  ],
  "repo_topology": null,
  "// repo_topology": [
    "How this run's deployables map onto git repos, when the platform pack offers a choice (`repo_topology.values` in pack.json). Content SDK offers two and BOTH are current: 'repo-per-deployable' (the head app is its own repo, a sibling of cm/) and 'monorepo' (ONE repo at cm/ holding authoring/ beside sites/<head-app-slug>/, with one root xmcloud.build.json). Asked in the /create-prd discovery grill, at intake, alongside the deploy-project + head-app names.",
    "RECORD IT EVEN WHEN THE OPERATOR TOOK THE DEFAULT. Every downstream stage re-resolves its pack brief with `--topology <this value>` and gets `workspace` + `repo_setup` already resolved for it; an absent field re-defaults to repo-per-deployable, so a monorepo run would provision, ship and publish against the wrong paths while every one of those stages reported green.",
    "Leave null when the platform declares a single fixed topology (marketplace) or none. Null means 'nothing to choose', not 'not asked yet'."
  ],
  "work_item_type": "prd",
  "// packs": "The resolved composition (platform-pack.md § 3). Written by M0 at intake. The nulls are recorded EXPLICITLY — \"platform\": \"none\" means resolved-and-the-answer-is-none, which an absent key cannot say. The slot was UNDECLARED until 2026-08-26 though step 18 and Validation both require it; runs worked only because manifest-write.mjs creates it on merge.",
  "packs": {
    "platform": "marketplace",
    "method": "greenfield",
    "audience": "developer",
    "resolved_by": "ask",
    "resolved_at": "2026-08-31T14:20:00Z",
    "signals": [
      "new product — nothing to inherit (no prior manifest) and nothing to detect (no tree); M0 ladder rung 4. Resolved by /start-flow and carried into /create-prd step 0."
    ]
  },
  "// workspace": "Names resolved from an active platform pack's `workspace.naming` (ONE owner — pack.json, not the command). `brand_name` is the ROOT of the derive chain: deploy_project_name / project_slug / head_app_slug all descend from it, so an unresolved brand_name leaves every one of them null rather than guessed. Declared 2026-08-27 — until then packs resolved these and the template had nowhere to put them, so each run invented a path. Slots exist for every name a shipped pack lists under `workspace.naming.tenant_facing` — `site_name` / `editing_host_name` (content-sdk) and `app_name` (marketplace) were missing until 2026-08-28, so the field the `required_at` rule is NAMED after could not be recorded where the rule checks it, and one run repurposed `brand_name` for a Cloud Portal app name.",
  "workspace": {
    "brand_name": null,
    "deploy_project_name": null,
    "project_slug": null,
    "head_app_slug": null,
    "project_root": null,
    "cm_code_base": null,
    "naming_source": "operator_supplied",
    "layout_deviation": null,
    "site_name": null,
    "editing_host_name": null,
    "app_name": "Link Health Lens"
  },
  "// variables": "Pack-declared build variables (`variables` in pack.json) once resolved — e.g. the Content SDK router, the Marketplace auth mode. Free-form by design: the owning pack defines the keys, so core cannot enumerate them.",
  "variables": {
    "mode": "client-side"
  },
  "status": "design_ready",
  "// work_item_type — schema 8 discriminator": [
    "work_item_type is the keystone discriminator set at intake by /create-prd (prd), /create-frd (frd), or /create-bug (bug). Every downstream command reads it to pick the weight-preset, the operator-gate count (prd=2, frd=1, bug=0+valves), and which stages to skip (bug skips architect+UI; frd skips architect+UI unless an ADR-worthy decision or UI scope exists).",
    "SAME ARTIFACT SLOTS, DIFFERENT FILL. canonical_artifacts.prd / prd_minimal hold the SPEC and SPEC-MINIMAL regardless of type — for an FRD they point at frd-NNN.md / frd-minimal-NNN.md; for a bug at bug-NNN.md / bug-minimal-NNN.md. The slot names stay `prd` / `prd_minimal` so downstream read paths (esp. Developer-08's `prd_minimal + task_breakdown`) are unchanged. The misnomer is intentional and documented.",
    "ID prefixes: PRD-NNN / FRD-NNN / BUG-NNN, per-product numbering namespaces. Branch names: prd-NNN / frd-NNN / bug-NNN.",
    "PROMOTION is re-classify + enrich, never restart: when a bug's root cause is a design flaw (not-contained valve) or an FRD balloons, work_item_type flips upward and the intake doc is enriched in place (bug repro/expected become FRD problem/AC; the minimal block carries forward). Record a stage_history entry { stage: \"promoted\", outcome: \"superseded\", one_line: \"BUG-003 promoted to FRD-004 — root cause is design, not a contained fix\" }.",
    "Legacy: schema-7-and-prior manifests have no work_item_type. Readers treat its absence as work_item_type: prd (the only type that existed)."
  ],
  "prd_input_mode": "prompt_only",
  "// platform_target": "RETIRED 2026-08-23 — superseded by packs{}. M0 no longer writes it. Ladder rung 2 (inherit-legacy) still READS it from pre-packs manifests to derive a composition; that is a one-way migration read, not a field to write.",
  "task_breakdown_style": null,
  "scope_dial": {
    "rigor": "light",
    "track": "minimal",
    "ui_variants": "2",
    "docs_at_ship": "auto-lite",
    "delivery_parallelism": "serial",
    "delivery_loop": "per_component",
    "autonomy": "custom",
    "gates": {
      "spec": false,
      "design": true,
      "build": false,
      "final": false,
      "go_live": false
    },
    "extra_stops": [],
    "content_authoring": null,
    "imagery": null,
    "external_reference": "cite",
    "architecture_budget": "mindful",
    "output_verbosity": "auto",
    "gate_presentation": null,
    "dual_review": "off"
  },
  "// scope_dial.gates + autonomy — configurable operator stops (schema 8+)": [
    "gates is the SOURCE OF TRUTH for which operator review-stops pause the run; a stage's hard stop fires iff its gate token is true. Five tokens: spec (/challenge-prd close — the reviewed spec; /create-prd inherits it only when the operator skips the review), design (/design winner-pick/confirm), build (END-OF-BUILD review at /test close — the operator's single look at the whole implementation via the Components Preview page; fires on content-sdk runs, where the build phase finishes before the post-authoring final gate; INERT on non-content runs, which review the build at the final gate below), final (/test for non-content OR /test-content for content — the last look), go_live (confirm before ANY outward/real-world mutation: /provision-sitecoreai real provisioning, /implement first real-tenant write, /test Components Preview fixture composition (QA 07), /author-content + /test-content live writes, and the /publish go-live). All default true = today's behavior. The build phase is autonomous either way — build/final is the ONLY operator stop during/after the build, never a per-component or mid-build one (rule 71-delivery-loop.mdc).",
    "extra_stops[] holds operator-named ADDITIONAL soft-stage stops promoted to hard (e.g. 'architect', 'task-breakdown', 'provision-sitecoreai', 'implement').",
    "autonomy is a DERIVED human-readable label, not the source of truth: auto_3_gates = all five gates true + no extra_stops (default, no opinion — the '3' is legacy shorthand for the review gates; build joins them on content-sdk runs and is inert elsewhere, so the effective review-stop count per run is still ~3); fully_autonomous = all five gates false + no extra_stops (TRULY ZERO — nothing pauses, live publish + real-tenant boundaries proceed unattended); custom = any other combination. Legacy manifests without the build token treat it as true (default). Legacy manifests with autonomy: extra_stops map to custom with the listed stops.",
    "GENUINE BLOCKERS ARE NOT GATES and always stop regardless of the gate set: a stage that cannot complete (build/publish-job failure), the bug can't-reproduce / not-contained valves, and any [BLOCKER]-tagged Operator Checkpoint item. fully_autonomous removes the REVIEW stops + boundary confirms, not error handling — there is no autonomous path past a hard failure, so it is surfaced.",
    "delivery_loop sets the build-phase TEST-LEG cadence (rule 71-delivery-loop.mdc). CHANGED 2026-07-29: the dial moves the QA leg ONLY — /code-review runs per component tranche under BOTH values (it is diff-local and cheap; batching it saves nearly nothing and lets a defect sit under N tranches of new work, whereas /test is integration-scoped and O(tranches x full-build) is the real cost driver). per_component (DEFAULT, 'standard') = the full {Developer->Code-Reviewer->QA} triad per tranche, looping back to the Developer on a Critical/Major or a test fail before the next component starts (best quality — small diffs, every component proven green; more time + tokens); once = a {Developer->Code-Reviewer} PAIR per tranche (implement -> review its diff -> bounded-3 loop-back -> COMMIT + RECORD at review-clean), then a SINGLE /test over the whole build once every tranche is review-clean, with bounded (3-cycle) whole-build loop-back whose fix diff is code-reviewed as normal (cheaper — one full suite/lint/build instead of N — at the cost of cross-component regressions surfacing late). Converges with delivery_parallelism=parallel, which is review-local + one post-merge QA by construction. INERT on single-tranche runs — a bug / small FRD / freeform run collapses to one implement→review→test pass regardless of this dial. Absent/legacy ⇒ per_component.",
    "content_authoring gates the Content SDK content phase (/discover-components → /author-content ⇄ /test-content → /publish): auto (default) = phase runs iff the active platform pack declares the content_authoring dial; yes = force the content phase; no = skip it entirely (/ship is terminal, the content-phase commands are not recommended).",
    "imagery is the empty-Image-field commissioning tier for /author-content (Sitecore channel), meaningful only when content_authoring runs: gemini (default) = real image generation via solo-sitecore-image-generation (brief → generate → operator review → upload → link → verify); mood = Claude-generated abstract/decorative SVG/gradient/generative-pattern imagery (no external service, no per-image cost) landed via the same skill; none = leave empty Image fields empty (CSS fallback). Absent/legacy ⇒ gemini (the standing 'imagery is a standard authoring step' default).",
    "external_reference governs whether the run may consult PEER PRODUCTS in this repo (velaro/verdure) for reference implementations: prohibited = build only from this product's own code + sanctioned shared knowledge (STOP-and-ask if a sibling would solve a wall); cite-required (default) = reference allowed but every borrowed peer-product pattern is logged (what + from where) + a reliance count reported at /ship; free = no logging. Never restricts the sitecore:* skills, node_modules .d.ts (rule 40), or the shared cm/. See rule 20-boundaries + glossary 'External reference'. Absent/legacy ⇒ cite.",
    "output_verbosity (rule 73-output-verbosity.mdc) controls MID-TURN chat narration, not artifacts on disk: asked at /create-prd Call 3 (Narration). auto (DEFAULT / Recommended) = each agent keeps its frontmatter pin (execution roles minimal, planning roles verbose) UNLESS autonomy is fully_autonomous, which forces minimal on every spawn; minimal = force minimal everywhere (cheapest autonomous runs); verbose = force verbose everywhere (debug / watch a run). Spawn prompts MUST include 'Output Level: <minimal|verbose>'. Slash override: verbosity=auto|minimal|verbose. Absent/legacy ⇒ auto."
  ],
  "content_policy": {
    "enforce_placeholder_restrictions": true
  },
  "// content_policy — guidance (schema 8+)": [
    "Set at /author-content kickoff for the Sitecore channel (the only channel with placeholders); inherited by /test-content. Absent/null on non-content runs and on legacy manifests — readers treat absence as enforce_placeholder_restrictions: true (follow the rules, the safe default).",
    "enforce_placeholder_restrictions (bool, default true): when true, the Content Author (14) composes ONLY renderings that the Marketer MCP `get_allowed_comps_by_ph(pageId, placeholder)` returns as allowed for the target placeholder — a desired-but-disallowed rendering is substituted with an allowed alternative or surfaced to the operator, never force-placed. When false (operator opted out), the author places renderings by best-fit regardless of the allowed set, but STILL records each placement that falls outside the allowed set as a deviation (so /test-content and the operator can see it).",
    "/test-content structure axis reads this flag: an out-of-allowed-set placement is a MAJOR structure finding under enforce=true, and a recorded (accepted) deviation under enforce=false."
  ],
  "ui_design": {
    "proposal_count": 2,
    "divergence": "moderate",
    "variant_paths": [
      "products/marketplace/link-health-lens/project-planning/ui-design/ui-design-20260831T143222Z-v1.md",
      "products/marketplace/link-health-lens/project-planning/ui-design/ui-design-20260831T143222Z-v2.md"
    ],
    "selected_variant_path": "products/marketplace/link-health-lens/project-planning/ui-design/ui-design-20260831T143222Z-v1.md",
    "poc_paths": [
      "products/marketplace/link-health-lens/project-planning/design-prototypes/poc-v1-prd000/index.html",
      "products/marketplace/link-health-lens/project-planning/design-prototypes/poc-v2-prd000/index.html"
    ],
    "selected_poc_path": "products/marketplace/link-health-lens/project-planning/design-prototypes/poc-v1-prd000/"
  },
  "source": {
    "kind": "raw_idea",
    "values": [],
    "analysis_mode": "greenfield",
    "source_analysis_artifact": null,
    "content_dump": null,
    "assets_bundle": null,
    "clone_scope_mode": null,
    "assets_root": null,
    "nav_depth": null,
    "parity_threshold": null,
    "parity_includes_authorability": null,
    "reproduce_improve_drop": null,
    "brand_deltas": null,
    "portability_caveats": null,
    "clone_handover_artifact": null,
    "parity_loop_enabled": false,
    "parity_scope_mode": null,
    "parity_scope_urls": [],
    "parity_max_runs": null,
    "parity_runs_executed": 0,
    "parity_stop_reason": null
  },
  "// source — clone scope guidance (schema 8+)": [
    "clone_scope_mode clarifies delivery intent when source-analysis ran in rebuild mode.",
    "Use home_plus_top5_primary_nav for a bootstrap parity slice; use full_website_clone when the run's definition of done is exact full-clone parity (route + component + content).",
    "When clone_scope_mode is full_website_clone, source-analysis should emit clone_handover_artifact and downstream planning commands (/architect, /task-breakdown) must load source_analysis_artifact + content_dump + assets_bundle + clone_handover_artifact.",
    "Use none when source-analysis did not run or analysis_mode is greenfield."
  ],
  "intake": {
    "source": null,
    "task_id": null,
    "issue_number": null,
    "routed_by": "/start-flow",
    "// note": "Set when the run was sourced via /triage (intake-pipeline). /ship reads this to close out the source at ship close (10-ship.md § 15a-intake for local, step 18 for github). Null/absent for runs started directly via /create-prd|frd|bug.",
    "// routed_by": "Which command routed this work item here (/triage, /start-flow, or null for a direct /create-*). Was hardcoded to \"/triage\" until 2026-08-28, so every --init seeded manifest claimed a provenance it did not have."
  },
  "canonical_artifacts": {
    "brain_dump": null,
    "prd": "products/marketplace/link-health-lens/project-planning/specs/prd-000.md",
    "prd_minimal": "products/marketplace/link-health-lens/project-planning/specs/prd-minimal-000.md",
    "adrs": [
      "products/marketplace/link-health-lens/project-planning/ADR/adr-0002-mode-a-client-side-only.md",
      "products/marketplace/link-health-lens/project-planning/ADR/adr-0003-rendered-page-scope-with-best-effort-attribution.md",
      "products/marketplace/link-health-lens/project-planning/ADR/adr-0004-extension-point-pages-contextpanel.md",
      "products/marketplace/link-health-lens/project-planning/ADR/adr-0005-status-set-with-display-precedence.md",
      "products/marketplace/link-health-lens/project-planning/ADR/adr-0006-structural-origin-three-way-label.md",
      "products/marketplace/link-health-lens/project-planning/ADR/adr-0007-blok-as-ui-framework.md",
      "products/marketplace/link-health-lens/project-planning/ADR/adr-0008-internal-link-resolution-is-a-two-call-composition.md"
    ],
    "architecture": null,
    "baseline": null,
    "task_breakdown": null,
    "task_breakdown_pre_qa": null,
    "qa_report": null,
    "code_review_report": null,
    "test_report": null,
    "content_test_report": null,
    "interrogation_ledger": "products/marketplace/link-health-lens/project-planning/plans/interrogation-discovery-20260831T143222Z.md",
    "challenge_ledger": null,
    "content_model": null,
    "content_examples": null,
    "ship_report": null,
    "friction_log": null,
    "status_index": "products/marketplace/link-health-lens/project-planning/STATUS.md"
  },
  "// canonical_artifacts — schema-7 deltas": [
    "implementation_runbook REMOVED in schema 7. The task breakdown's § 0 (Quick orientation) now carries target directory + canonical inputs + delivery order. Reading a schema-6 manifest? Treat any present implementation_runbook entry as legacy/archived; do not produce new ones.",
    "baseline ADDED in schema 7. Optional path to project-planning/baseline.md (architecture baseline) promoted from stable ADRs after the 5-ADR threshold per `/architect` baseline-promotion gate. Null when no baseline has been promoted.",
    "status_index ADDED in schema 7. Path to project-planning/STATUS.md (per-product PRD index). Auto-generated by /create-prd kickoff and /ship close."
  ],
  "friction_log": {
    "path": null,
    "min_severity": "medium",
    "enabled": true
  },
  "stage_history": [
    {
      "stage": "discovery",
      "agent": "02-product-manager",
      "outcome": "completed",
      "started_at": "2026-08-31T14:20:00Z",
      "ended_at": "2026-08-31T14:32:00Z",
      "notes": "light rigor, 5 questions, closed on budget. Two push-backs; the second changed the product (Mode A removes external checking)."
    },
    {
      "stage": "discovery_summary",
      "agent": "02-product-manager",
      "outcome": "confirmed",
      "started_at": "2026-08-31T14:32:00Z",
      "ended_at": "2026-08-31T14:36:00Z",
      "notes": "Recap confirmed without changes, including slug link-health-lens and probing assumption A2 rather than dropping it."
    },
    {
      "stage": "spec_ready",
      "agent": "02-product-manager",
      "outcome": "completed",
      "started_at": "2026-08-31T14:36:00Z",
      "ended_at": "2026-08-31T14:45:00Z",
      "notes": "prd-000.md (18 headings) + mechanically extracted prd-minimal-000.md (7 sections) + 3 new ADRs. track=minimal, so ADRs carry the architecture."
    },
    {
      "stage": "spec_ready",
      "agent": "02-product-manager",
      "outcome": "superseded",
      "notes": "The drafted spec was replaced by the reviewed one at /challenge-prd close."
    },
    {
      "stage": "spec_challenged",
      "agent": "02-product-manager",
      "outcome": "completed",
      "started_at": "2026-08-31T14:50:00Z",
      "ended_at": "2026-08-31T15:15:00Z",
      "one_line": "light review; 13 challenges (3 blocker / 6 major / 4 minor); merged into prd-000.md",
      "deliverables": [
        "products/marketplace/link-health-lens/project-planning/specs/prd-000.md",
        "products/marketplace/link-health-lens/project-planning/specs/prd-minimal-000.md",
        "products/marketplace/link-health-lens/project-planning/ADR/adr-0005-status-set-with-display-precedence.md",
        "products/marketplace/link-health-lens/project-planning/ADR/adr-0006-structural-origin-three-way-label.md"
      ],
      "escalations": [
        "G3 — phase 1 needs a named tenant, a settled app_name and operator-guided App Studio registration; nothing in the run supplies them. Operator chose to settle it before /architect, so this close is a hard stop."
      ]
    },
    {
      "stage": "architecture_ready",
      "agent": "04-software-architect",
      "outcome": "completed",
      "started_at": "2026-08-31T15:40:00Z",
      "ended_at": "2026-08-31T16:05:00Z",
      "one_line": "minimal track — no blueprint; ADR-0007 (Blok, from required_adrs) + ADR-0008 (two-call resolution, from the SDK gate)",
      "deliverables": [
        "products/marketplace/link-health-lens/project-planning/ADR/adr-0007-blok-as-ui-framework.md",
        "products/marketplace/link-health-lens/project-planning/ADR/adr-0008-internal-link-resolution-is-a-two-call-composition.md"
      ],
      "escalations": [
        "SDK contract gate initially unsatisfiable on this platform; resolved by a types-only install. The ordering defect stays open against the pack."
      ]
    },
    {
      "stage": "baseline_promoted",
      "agent": "04-software-architect",
      "outcome": "skipped",
      "one_line": "Operator-recommended skip; 7 ADRs but none has survived a second PRD and ADR-0006 is in flux behind probe A4"
    },
    {
      "stage": "ui_ready",
      "agent": "05-ui-designer",
      "outcome": "completed",
      "started_at": "2026-08-31T16:25:00Z",
      "ended_at": "2026-08-31T17:25:00Z",
      "one_line": "2 variants at moderate divergence; operator picked v1 'Triage by severity' at the design gate",
      "deliverables": [
        "products/marketplace/link-health-lens/project-planning/ui-design/ui-design-20260831T143222Z-v1.md",
        "products/marketplace/link-health-lens/project-planning/ui-design/ui-design-20260831T143222Z-v2.md",
        "products/marketplace/link-health-lens/project-planning/design-prototypes/poc-v1-prd000/",
        "products/marketplace/link-health-lens/project-planning/design-prototypes/poc-v2-prd000/"
      ],
      "escalations": [
        "Blok publishes no `warning` semantic role (OQ-A, open)",
        "Two measured contrast failures on shadcn stock tokens; Blok's real values unmeasured (open)"
      ]
    }
  ],
  "// stage_history — schema 7 contract": [
    "Schema 7 replaces the prose `notes` essay with structured fields. Required: stage, agent, entered_at, exited_at, outcome, one_line, deliverables. Optional: escalations[] (mirror of operator_attention items raised at this stage), error (on failure), notes (free-form ESCAPE HATCH — keep ≤2 sentences; prefer one_line + escalations).",
    "`outcome` values:",
    "  completed — stage finished as intended",
    "  failed    — stage hit an unrecoverable error; error field populated",
    "  skipped   — stage was explicitly bypassed (e.g. user said 'skip discovery')",
    "  superseded — a later stage replaced this stage's outputs (e.g. PRD revision after critical review)",
    "  partial   — stage completed but with caveats (e.g. some tasks deferred; one_line states what)",
    "`one_line` is the at-a-glance summary /status reads. Keep tight; details belong in deliverables[] and the artifact itself.",
    "`deliverables[]` lists EVERY file the stage wrote or updated by repo-relative path. /status uses this to surface 'what changed at this stage'.",
    "`escalations[]` mirrors operator_attention entries raised AT this stage. The top-level operator_attention[] is still the SoT for cross-product roll-up; this per-stage slice is for forensic queries ('what concerns did /architect raise?').",
    "Backward compatibility: schema-6 manifests have stage_history entries with a freeform `notes` field instead of `one_line` + `deliverables` + `escalations`. Readers should treat schema-6 `notes` as the one_line equivalent and skip the structured fields."
  ],
  "implementation": {
    "mode": "pipeline",
    "target_directory": "site",
    "branch_name": "prd-000",
    "git_remote": "create",
    "remote_url": "https://github.com/Chris1415/Sitecore.Plugin.LinkHealthLens.git",
    "status": "not_started",
    "resumed_from_stage": null
  },
  "next_action": "run /task-breakdown — Lead Developer task list + QA enrichment; section 5 MUST carry an explicit task owning the port of the POC's visual layer, and section 8 a per-tranche pixel compare against poc-v1-prd000",
  "// next_action — crash-resilience / resume field (schema 8+)": [
    "Written by EVERY stage/tranche/component close (alongside the stage_history entry + git commit — the per-unit checkpoint, rule 71-delivery-loop.mdc / 72-orchestrator-discipline.mdc). States the explicit next step in plain terms so a fresh orchestrator (after a crash/disconnect) knows exactly where to continue WITHOUT re-deriving it from context.",
    "/resume reads this after reconciling disk + git against the manifest (see .agent/commands/project/resume.md): diff reality → update the manifest to match → continue from next_action.",
    "During the per-component delivery loop, next_action names the current component + leg (e.g. 'code-review the Hero tranche' → 'test the Hero tranche' → 'implement the Footer tranche'), so a mid-loop crash resumes the exact triad member.",
    "Backward-compatible: absent on legacy (schema-7-and-prior) manifests and on runs that predate this field — readers treat its absence as 'resume from the last stage_history entry' (the pre-next_action behavior). Never load-bearing for existing runs."
  ],
  "smoke_outcomes": {},
  "// smoke_outcomes — category guidance": [
    "host_frame_smoke: 5-axis pixel comparison vs POC (canonical, see the marketplace-sdk-host-frame-testing skill). Verifies 'did we ship the design'.",
    "live_walkthrough: editor-driven exploration of the installed app on a real tenant for 5+ minutes. Verifies 'does the design actually work for the user'. REQUIRED when the stage brief declares requires_smoke with a host-framed visual_harness, before status can be 'shipped' (vs 'shipped_with_caveats'). Component Usage Atlas's S8/S9/S10/S11 — and S22/S23 — were all live-walkthrough findings that the host-frame smoke could not have caught.",
    "deploy: production deployment outcome (Vercel, etc.).",
    "registration: marketplace registration outcome (Cloud Portal app entry + scopes)."
  ],
  "operator_attention": [
    {
      "stage": "create-prd",
      "type": "decision",
      "item": "The tenant-facing app_name is unresolved and Cloud Portal App Studio registration must happen BEFORE /implement — ClientSDK.init() handshakes with the portal and there is no registration API (probe 0a, refuted 2026-08-15). This is operator-guided by construction, so it stops the run regardless of gates.go_live being off.",
      "status": "open"
    },
    {
      "stage": "create-prd",
      "type": "assumption",
      "item": "A2 — path-by-url can distinguish 'page missing' from 'page exists but unpublished'. Unconfirmed, and US-3 (unpublished-target detection) is a headline feature resting on it. AC-3.3 pre-commits a merged 'not found or not published' label so a refutation degrades the feature honestly instead of mislabelling.",
      "status": "open"
    },
    {
      "stage": "create-prd",
      "type": "assumption",
      "item": "A1 — the page-HTML endpoint returns rendered HTML, not layout JSON. If it returns layout, link extraction is different work and tranche 2 is reshaped. Probed in tranche 1.",
      "status": "open"
    },
    {
      "stage": "create-prd",
      "type": "assumption",
      "item": "A3 — chrome links attribute to an owning datasource often enough for best-effort to read as partial rather than blank. Threshold ~50%, measured as M3 at phase 4 exit; below it is a UX re-decision for the operator.",
      "status": "open"
    },
    {
      "stage": "create-prd",
      "type": "concern",
      "item": "Name/scope tension kept deliberately: the app is called Link Health Lens and never checks the health of an external link. Mitigated in-product (UI is forbidden from saying broken/dead/404/unreachable; metric M5 greps for it at every /code-review) rather than by renaming. Operator confirmed the name at the discovery checkpoint.",
      "status": "open"
    },
    {
      "stage": "create-prd",
      "type": "concern",
      "item": "PRD-000 is 560 lines against the template's ~400 soft cap. The cap's prescribed remedy is phasing, which § 12a applies; the volume is 8 stories x acceptance criteria + 17 FRs + a self-contained Developer slice. Flagged rather than trimmed, because trimming would have cut acceptance criteria.",
      "status": "open"
    },
    {
      "stage": "create-prd",
      "type": "concern",
      "item": "gates.spec is off, so /challenge-prd will interrogate and then close SOFT rather than stopping for approval. The spec reaches /architect without a human confirmation step — that is the dial the operator set, recorded here so it is visible rather than inferred.",
      "status": "open"
    },
    {
      "stage": "challenge-prd",
      "type": "decision",
      "severity": "blocker",
      "item": "[BLOCKER] G3 — before /architect: name the tenant, settle app_name, complete Cloud Portal App Studio registration (human Org Admin, no API — probe 0a), and name a known-bad page plus a known-good control page. Phase 1 is blocking and every phase exit is real-tenant-bound; without these the run reaches /implement and stops dead with nobody summoned. Operator chose 'settle it now' over an extra_stops entry.",
      "status": "open"
    },
    {
      "stage": "challenge-prd",
      "type": "assumption",
      "severity": "warn",
      "item": "A4 (new) — the page-HTML endpoint exposes a structural signal sufficient to separate chrome from page content. The entire three-way label rests on it and nobody has seen it. T0 probe (f) is blocking; a refutation is an operator re-decision (R8), NOT an implementation fallback — reverting to attribution-derived origin would reinstate the unfailable M3 that ADR-0006 exists to prevent.",
      "status": "open"
    },
    {
      "stage": "challenge-prd",
      "type": "concern",
      "severity": "info",
      "item": "The spec grew 561 -> 658 lines against the ~400 soft cap. The PM removed every duplication it introduced; the residue is requirement text the review obliged (6 new ACs, 2 new FRs, the status-set model, A4, R8, two probe questions, the phase preconditions). Further cuts would remove decisions, not padding. Operator call whether to trim.",
      "status": "open"
    },
    {
      "stage": "challenge-prd",
      "type": "concern",
      "severity": "info",
      "item": "G1 and G2 were both blockers caused by the SAME thing: the mid-discovery reframe from broken-link checking to internal-link integrity was complete in the narrative sections and incomplete in the requirements layer. A spec can read as fully reframed in section 1 and still instruct an implementer to build the old product in section 7.",
      "status": "open"
    },
    {
      "stage": "challenge-prd",
      "type": "decision",
      "severity": "blocker",
      "item": "[BLOCKER] G3 RESIDUAL — accepted by operator YES on 2026-08-31, run proceeds to /architect with it open. Before /implement: complete Cloud Portal App Studio registration for app 'Link Health Lens' on the devex tenant (human Org Admin, no API), name a known-bad page and a known-good control page, and supply a CM credential — none is present in the run environment. T0 is blocking and every phase exit is real-tenant-bound, so this stalls /implement rather than degrading it.",
      "status": "open"
    },
    {
      "stage": "architect",
      "type": "concern",
      "severity": "blocker",
      "item": "[BLOCKER] SDK contract verification gate cannot be satisfied at /architect on platform:marketplace. It requires a .d.ts citation or a captured real-tenant shape per SDK call; the platform scaffolds nothing before /implement (so there is no node_modules) and the probe that would capture shapes is inside /implement behind an unfinished registration. Critical trigger (a) in this command's output rules. Either the operator overrides, or the SDK packages get installed early purely to read their types.",
      "status": "open"
    },
    {
      "stage": "architect",
      "type": "decision",
      "severity": "warn",
      "item": "Baseline-promotion gate: 6 non-boilerplate ADRs, threshold >=5 met and no baseline.md exists. RECOMMEND SKIP — the gate promotes decisions that have survived 2+ PRDs, and every ADR here is hours old from a single PRD, with ADR-0006 in active flux (probe A4 could refute it). Promoting now would freeze a constitution that has never been tested.",
      "status": "open"
    },
    {
      "stage": "architect",
      "type": "concern",
      "severity": "warn",
      "item": "A2 was REFUTED IN SHAPE by the declared types before a single line of code was written: path-by-url carries no publish state, so missing-vs-unpublished needs a second call (ADR-0008). US-3 survives and AC-3.3's fallback label does not need to fire, but the cost is TWO calls per internal link against a <=3s p50 budget for 150 anchors — the task breakdown must plan de-duplication by resolved itemId.",
      "status": "open"
    },
    {
      "stage": "architect",
      "type": "concern",
      "severity": "warn",
      "item": "getLivePageState declares its 200 body as `unknown`, so rule 40 cannot be satisfied by type alone for that one call. A 404 must be handled as DATA, not as an error — a generic retry/report handler would turn the app's headline finding into an infrastructure warning, which is the most likely way to implement this wrong.",
      "status": "open"
    },
    {
      "stage": "design",
      "type": "decision",
      "severity": "warn",
      "item": "OQ-A (both variants) — Blok publishes NO `warning` semantic role (primary / destructive / muted / accent / background / foreground; accent is a background role). Neither designer invented one. Severity rides on glyph + text chip + rail, so both designs stay legible and AA without it. If a warning token is wanted it is a Blok-side request, not a build task.",
      "status": "open"
    },
    {
      "stage": "design",
      "type": "concern",
      "severity": "warn",
      "item": "Two MEASURED contrast failures, carried into the build regardless of winner: shadcn's stock --destructive #ef4444 is 3.76:1 on white and fails AA for the 13px row label (v1 shims one step darker to 4.83:1); muted-foreground on muted is 4.40:1, so neither design ever pairs them. v2 additionally measured the default `ring` token at 2.56:1 against background — under the 3:1 focus-indicator floor. Blok's REAL token values must be measured before ship; these are shadcn stock readings.",
      "status": "open"
    },
    {
      "stage": "design",
      "type": "decision",
      "severity": "warn",
      "item": "OQ-B (v2 only) — v2's overview ruler has no Blok primitive. It is composed from Blok semantic tokens plus @blok/tooltip, which under ADR-0007 is an opt-out needing a recorded decision. If refused, the ruler goes and v2 still stands with weaker wayfinding. Only decidable if v2 wins.",
      "status": "open"
    },
    {
      "stage": "design",
      "type": "decision",
      "severity": "warn",
      "item": "OQ-C (v2 only) — AC-7.2's reading: must the chrome/unattributed string be painted on EVERY row, or only on rows carrying a status? v2 paints it on rows with a status member and carries it via icon + aria-label on quiet rows. Painting it everywhere grows quiet rows a line and lengthens the walk by ~40%. Needs a ruling, not a guess.",
      "status": "open"
    },
    {
      "stage": "design",
      "type": "assumption",
      "severity": "warn",
      "item": "v2's region banding depends on Assumption A4 (a structural chrome/content signal from the page-HTML endpoint, T0 probe f). If A4 is refuted the banding and canvas affinity die; the ruler, walked markers, filters and row anatomy survive. v1 does not depend on A4 for its grouping, only for the three-way label both share.",
      "status": "open"
    },
    {
      "stage": "design",
      "type": "concern",
      "severity": "info",
      "item": "OQ-B and OQ-C are MOOT — both were v2-only (the ruler's Blok opt-out, and AC-7.2's painting rule as v2 read it). v1 won, so neither needs a ruling. Recorded rather than silently dropped, since both were raised to the operator as open decisions.",
      "status": "resolved"
    },
    {
      "stage": "design",
      "type": "concern",
      "severity": "warn",
      "item": "v1's urgency-first ordering leads the editor to what will hurt rather than what they can fix here — on a chrome-heavy page, 12 findings and none actionable (state-chrome-only.html). The chrome counts on the verdict sub-line and per group are LOAD-BEARING MITIGATION, not decoration. An implementer who drops them as visual noise reinstates the trade-off unmitigated.",
      "status": "open"
    }
  ],
  "// operator_attention — guidance": [
    "Mirrors the Operator Checkpoint block (see .agent/glossary.md § Operator Checkpoint) so /status can roll up unresolved attention items across products.",
    "Append-only: never edit a prior entry in place. To mark an item resolved, append a new entry with kind: 'resolved' and references: <prior raised_at>.",
    "Severity guidance: 'blocker' means the next stage cannot proceed; 'warn' means the run can continue but the operator should know; 'info' is FYI.",
    "Kind values map 1:1 to the Operator Checkpoint sections: decision → Decisions needed, assumption → Assumptions made, concern → Concerns / risks, question → Open questions, resolved → resolution of a prior entry."
  ],
  "archive": {
    "// guidance": [
      "RETIRED (schema 8+). Both archive triggers are gone: supersession archiving (moved a superseded task-breakdown / ui-design / architecture into archive/ at the next stage write) and the post-ship scaffolding sweep (/ship step 15b). Neither moves anything now, and notes.archive_skip is retired with them.",
      "Replaced by ARTIFACT DURABILITY TIERING, enforced declaratively by project-planning/.gitignore (seeded at /create-prd step 4a from .agent/templates/project-planning-seed/.gitignore). Same intent, no file movement.",
      "TIER 1 — TRACKED (the durable record): spec (prd/frd/bug-NNN.md), ADR/, baseline.md, plans/ship-report-*.md, plans/*probe-findings*.md, captures/, spike-notes/, smoke/, source-analysis/, STATUS.md, tasks.md, workflow/run-<id>.json.",
      "TIERS 2-4 — UNTRACKED (produced and read by the pipeline exactly as before, never committed): *-minimal-NNN.md (compiler output); task-breakdown / qa-report / code-review / test-report, architecture/, brain-dumps/, ui-design/, design-prototypes/ (run-scoped working material); workflow/current-run.json (mutable machine state — byte-identical to the latest run-<id>.json, which IS tracked).",
      "canonical_artifacts.* paths are therefore STABLE — no stage rewrites them to an archive location, so a later /document or design-polish run resolves the same path the producing stage wrote.",
      "Legacy products may still have an archive/ folder holding durable artifacts the retired sweep trapped there (probe findings, smoke notes, even ship reports). /ship § 15b-promote rescues those back to plans/ before the folder is untracked. See .agent/manifests/README.md § 'Artifact durability tiers'."
    ]
  },
  "// notes": "OBJECT, not an array. 17 contract citations address it as notes.<key> — base_branch, program, sdk_surface_split, split_override, critical_review, scope_dial, composition_detected_from and more. The template declared an ARRAY, which produced real manifests of both shapes in the same workspace. Corrected 2026-08-24 after a throwaway run had to pass --replace notes to write a documented key.",
  "notes": {
    "base_branch": "main",
    "discovery": "completed",
    "discovery_level": "light",
    "discovery_questions": 5,
    "discovery_ledger": "products/marketplace/link-health-lens/project-planning/plans/interrogation-discovery-20260831T143222Z.md",
    "discovery_summary": "confirmed",
    "sitecore_discovery": "completed",
    "sdk_surface_split": "phased",
    "sdk_surface_phases": 4,
    "dials_preset_by_pack": {},
    "dials_dropped_by_pack_filter": {
      "content_authoring": "no active pack owns /author-content",
      "imagery": "no active pack owns /author-content"
    },
    "grill_relay": "The grill ran inline via orchestrator relay rather than inside the spawned PM: an interactive multi-turn interrogation cannot interleave with the operator from a background subagent. The constructive spawn received the ledger PATH only (rule 72).",
    "product_reframe": "Discovery Q4/Q5 changed the product. variables.mode = client-side removes every outbound network check, so broken-link / redirect-chain / DNS detection is PERMANENTLY out of scope and the spec is internal-link integrity. See ADR-0002.",
    "critical_review": "completed",
    "critical_review_level": "light",
    "critical_review_questions": 4,
    "critical_review_ledger": "products/marketplace/link-health-lens/project-planning/plans/interrogation-challenge-20260831T150500Z.md",
    "critical_review_gaps": "13 total — 3 blocker, 6 major, 4 minor. Triage: 4 operator-decision (grilled), 8 self-resolvable (fixed without asking), 1 verifiable (routed to the T0 probe as question (e)).",
    "tenant": "devex — the DevEx org test tenant, named by the operator 2026-08-31 in answer to blocker G3",
    "app_name_rationale": "Kept as 'Link Health Lens' rather than renamed to match the internal-link-integrity scope. Confirmed once at the discovery checkpoint with the scope change known; required_at is before_scaffold, so the name enters tenant state at App Studio registration and a rename after that costs a re-registration. The honesty risk is handled in-product (global word ban + metric M5), not by the tile label.",
    "g3_disposition": "PARTIALLY settled, remainder accepted by the operator with an explicit YES. Settled: tenant (devex) + app_name (Link Health Lens). Still outstanding before /implement: the operator-guided Cloud Portal App Studio registration (human Org Admin, no API), a named known-bad page and a named known-good control page, and a CM credential — no SITECORE_CM_* var is present in the run environment.",
    "planning_track": "minimal",
    "adr_density_split": "n/a — 1 new ADR in this /architect run (ADR-0007), well under the 5-ADR density gate",
    "required_adrs_check": "Pack declared 3 required_adrs. Scaffold choice -> ADR-0002 (pre-existing). Extension-point set -> ADR-0004 (pre-existing). Blok as UI framework -> MISSING, written now as ADR-0007. The gate caught a genuine undocumented default: no section of PRD-000 named a UI framework, so /implement would have inherited whatever the scaffold emitted.",
    "sdk_gate_blocked": "Step 2a-bis / Validation demand a .d.ts citation or a captured real-tenant shape for every SDK call the architecture relies on. NEITHER IS OBTAINABLE AT THIS STAGE on this platform: repo_setup.deployable_baseline is null so no scaffold runs before /implement, which means no node_modules and no .d.ts; and the T0 probe that would capture real shapes lives inside /implement phase 1, behind an App Studio registration that has not happened and a CM credential that is not present. This is an ORDERING DEFECT IN THE PACK, not a defect in this spec — PRD-000 already marks all five surfaces unverified and pre-commits their capture to T0.",
    "pack_finding_candidate": "platform:marketplace has no stage between /architect and /implement that can produce a .d.ts, yet /architect's own validation requires one for a Sitecore-track run. F36 item K fixed the naming half of probe-first-vs-scaffold-first (required_at: before_scaffold); this is the SDK-shape half and it is still open.",
    "sdk_gate": "SATISFIED by a types-only install at /architect (operator-chosen option 1). @sitecore-marketplace-sdk/client@0.3.6 + /xmc@0.4.2 installed at the product root — NOT in site/, so the scaffold's target stays empty and /implement does not hit the non-empty-target path. package.json + package-lock.json are committed, so every .d.ts citation is reproducible.",
    "sdk_gate_findings": "The install immediately refuted a spec assumption. (1) A1 RESOLVED from the type: Agent.PageHtmlResponse = { pageId: string; html: string } — the endpoint returns HTML as a string, not layout JSON, so the extraction design stands. (2) A2 REFUTED IN SHAPE: the real key is xmc.agent.pagesGetPagePathByLiveUrl, keyed on live_url, and its response carries NO publish-state field — one call cannot separate missing from unpublished. The distinction needs a second call, xmc.pages.getLivePageState, where a 404 IS the not-published signal. Recorded as ADR-0008. (3) getLivePageState's 200 body is declared `unknown`, so rule 40 cannot be satisfied by type alone — T0 probe (b) is narrowed to capturing that body rather than asking whether the distinction exists. (4) `language` is a REQUIRED query param on both calls, which is the language axis G4 raised at /challenge-prd.",
    "baseline_promoted": "skipped — 7 non-boilerplate ADRs, threshold met, but the gate promotes decisions that have survived 2+ PRDs and every ADR here is hours old from a single PRD, with ADR-0006 in active flux behind probe A4. Promoting now would freeze an untested constitution.",
    "pack_fix_agreed": "Operator chose option 3 as the standing fix: give platform:marketplace a sanctioned types-only install step before /architect, so the next Marketplace run does not meet this wall. To be applied to the pack, not to this product.",
    "design_divergence_rationale": "moderate, not radical — ADR-0007 fixes typography, colour and component vocabulary to Blok, so the only legal axes of divergence are layout, information hierarchy and finding-row presentation. v1 'Triage by severity' groups by what the editor should do; v2 'Walk the page in order' lists in document order so the panel mirrors the canvas. Genuinely different organising principles inside one token system.",
    "design_brief_skills_gap": "PACK FINDING CANDIDATE. The /design brief for platform:marketplace carries constraints.blok-first-design ('design with Blok primitives; inventing custom markup where a Blok component exists is an opt-out needing an ADR') and iframe-viewport — but stages.design.skills is EMPTY. So the stage that ENFORCES design-system conformance is handed the mandate and no catalogue of what the system contains. 04b-design.md's own design-system-enforcement paragraph says to put the brief's 'skills + constraints' in the sub-agent prompt; the skills half resolves to nothing. Same family as F38: a requirement with no declared means to satisfy it. Worked around by naming blok-components / blok-theming / blok-marketplace-integration explicitly in both spawn prompts.",
    "design_critic_skipped": "Step 4 scoring is optional and was skipped deliberately. The two variants differ on ONE stated axis (urgency-first vs position-first) and a 1-10 score across five dimensions would average that away. Both self-verified by rendering; each found and fixed a real defect in its own build, and v2 caught a factual error in v1's spec. The operator opening both clickdummies is the better instrument.",
    "design_poc_contract_asymmetry": "v1 ships 45 discrete HTML frames; v2 ships 5 files plus URL-parameterised states (?view=row-open&n=89). v2's argument: 150 row instances would mean 150 files, and a query-param frame is equally specific and equally openable from file://. That satisfies the reviewability the click-targets rule exists for, but it is a different contract SHAPE, and /task-breakdown's Lead Developer plus QA's smoke both read that file. If v2 wins, the smoke plan must state that a post-state is a URL, not a path.",
    "design_v1_spec_corrected": "v1's section 1 described v2 as ordering 'by fixability, chrome as a labelled tail' — the PRD section 11 SUGGESTED axis, not what v2 was built as. v2 flagged it; corrected in place before the operator read both, since an uncorrected line would have made the side-by-side comparison false.",
    "design_winner": "v1 'Triage by severity' — picked by the operator at the design gate, not auto-selected. Robustness note worth carrying: v1's GROUPING does not depend on Assumption A4 (the structural chrome/content signal from T0 probe f); only the shared three-way label does. v2's region banding would have died with an A4 refutation, so the pick is also the more probe-resilient of the two.",
    "design_poc_gate_script": "poc-v1-prd000/verify-poc.mjs is a 35-assertion gate that walks every reachable state and fails on a dangling href. It found the build's one real navigation defect. Run it at every tranche exit, not once at design close — a frame that stops honouring an ADR fails it."
  },
  "discovery_mode": "relayed",
  "discovery_questions_asked": 5,
  "// discovery_mode": "How the grill actually reached a human, if it did. `unattended` MUST carry discovery_questions_asked: 0 — a non-zero count there means answers were invented, which the interrogation protocol prohibits absolutely. Added 2026-08-26 after six runs each improvised their own free-text note because no field existed, and a fabricated ledger was indistinguishable from a real one on the manifest.",
  "// source.portability_caveats": "NULL, never []. `method:clone` states: \"An empty caveat list is a CLAIM, not a default. On a real clone, empty is evidence the check did not run.\" The template shipped `[]` for one day (2026-08-27), so --init wrote the exact value the owning pack forbids and a run that left it would have read as \"we checked and found none\" while having asked nobody.",
  "architecture": {
    "extension_points": [
      {
        "extension_point": "xmc:pages:contextpanel",
        "route_url": "/pages-context",
        "registered": false
      }
    ]
  }
}
