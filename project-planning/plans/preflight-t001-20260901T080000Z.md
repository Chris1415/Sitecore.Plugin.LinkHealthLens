---
document_type: preflight_check
task_id: T001
generated_at: 2026-09-01T08:00:00Z
run_manifest: project-planning/workflow/run-20260831T143222Z.json
---

# T001 — § 12a preconditions, verified before TR-0 continues

| # | Item | Status | Evidence |
|---|------|--------|----------|
| 1 | Tenant is `devex` | **present** | `manifest.provisioning.cm_tenant = developerex211b-hahnsolo3097-cm7744`. Re-confirmed live: `node .agent/scripts/assert-tenant.mjs --org devex --env products/head-application/hahn-solo/cm/.env --json` minted a token whose own `tenant_name` claim asserts `developerex211b-hahnsolo3097-cm7744` (2026-09-01 08:0x). |
| 2 | `app_name` fixed | **present** | `manifest.workspace.app_name = "Link Health Lens"`; App Studio display name `Link Health` (`manifest.provisioning.display_name`). |
| 3 | App Studio registration complete, extension point `xmc:pages:contextpanel` @ `/pages-context`, `resourceAccess[]` names devex | **present** | `manifest.provisioning.app_registered = true`, `extension_point`/`route_url` match; `app_registration_org = "Sitecore Dev Relations (devrel)"`, `app_shared_with = ["devex"]` — registration lives in devrel, the app is **shared** into devex where it is confirmed working (operator-verified, per commit `629a10f`). This is why the out-of-band credential must be the DEVEX pair, not devrel — recorded in `notes.estate_resolution`. |
| 4 | Named known-bad page + named known-good control page | **present** | `smoke_page_known_bad = /sitecore/content/Zephira-Brand/Zephira/Home`, `smoke_page_known_good_control = /sitecore/content/Koven-Brand/Koven/Home`. Provenance: orchestrator-proposed, operator-delegated ("go with what you think is fine") — not invented, not directly named by the operator. Manifest flags both as **proxies**: Koven Home is small-surface-not-verified-clean (control moves if TR-3 finds anything on it, M4 never relaxes); Zephira's known defects are LIVE route-path divergences (rule 85) that may be invisible to this CM-side checker, with Velaro Home as the named fallback for anchor volume. |
| 5 | CM credential / `sitecoreContextId` reachable | **present** | `SITECORE_CM_CLIENT_ID_DEVEX` / `_SECRET_DEVEX` in `products/head-application/hahn-solo/cm/.env`, read-only. Minted and tenant-asserted live (see #1). Not logged, not printed — `assert-tenant.mjs`'s own discipline. |

## Verdict

**All five items present. No HARD STOP.** T002 (scaffold) may proceed.

## Note on scope

Item 3's registration/sharing nuance (devrel registration, devex install) was resolved by the orchestrator prior to this Developer run (manifest `stage_history` "provisioning" commit `629a10f`, 2026-09-01) — restated here, not re-decided. Items 4 and 5 read the same way: settled upstream, confirmed present, not re-litigated.
